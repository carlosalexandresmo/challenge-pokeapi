import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const primaryDark = Color(0xFF24483c); //verde escuro (backgrounds de menu, header)
  static const primary = Color(0xFF41907d); //verde médio (botões, hover de links)
  static const primaryLight = Color(0xFF97c0ae); //verde claro (background geral, highlights, links sobre o verde escuro)
  static const accent = Color(0xFFd29425); //laranja (bom sobre o verde escuro, botões secundários)
  static const accentButton = Colors.amber;
  static const textColorPrimary = Color(0xFF434349); //quase preto (para textos)
  static const textColorSecondary = Color(0xFF959cb6); //cinza (para textos / links)

}